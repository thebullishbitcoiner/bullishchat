{
  "name": "bullishchat",
  "version": "0.10.3",
  "description": "Private messaging on Nostr. Sign in with a browser extension; keys never leave your wallet.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@getalby/lightning-tools": "^8.1.0",
    "nostr-tools": "^2.18.0"
  }
}
